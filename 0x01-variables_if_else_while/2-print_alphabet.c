/**
 * Main - starting point of program
 * Return - terminates main function
*/


#include <stdio.h>

int main(void)
{
char alpha = 'a';
while (alpha <= 'z')
{
	putchar(alpha);
	alpha++;
}
 putchar("\n");
return (0);
}
